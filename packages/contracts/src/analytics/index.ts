/**
 * Analytics types and schemas.
 *
 * This module contains types for:
 * - Metric snapshots with task throughput, agent utilization,
 *   approval latency, error rate, and token consumption
 * - MetricEventEmitter/MetricListener subscription interfaces
 * - Alert rules with configurable thresholds
 * - Alert state, events, and notification payloads
 * - Audit trail query filters and entry schemas
 */

export * from "./types";

