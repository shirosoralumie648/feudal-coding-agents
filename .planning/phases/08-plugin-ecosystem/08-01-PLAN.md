---
phase: 08-plugin-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/plugins/types.ts
  - packages/contracts/src/plugins/sdk.ts
  - packages/contracts/src/plugins/types.test.ts
autonomous: true
requirements:
  - PLG-03
  - PSC-02
must_haves:
  truths:
    - "Existing Phase 5 plugin manifests remain valid"
    - "Plugin manifests can declare explicit permissions"
    - "SDK helpers create ACP worker and workflow step extensions"
    - "Compatibility evaluation reports compatible/incompatible/unknown"
    - "Marketplace entries and security reviews have shared schemas"
---

<objective>
Extend plugin contracts and SDK helpers for local ecosystem use.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add plugin ecosystem contracts and SDK helpers</name>
  <files>packages/contracts/src/plugins/types.ts, packages/contracts/src/plugins/sdk.ts, packages/contracts/src/plugins/types.test.ts</files>
  <acceptance_criteria>
    - Tests are written and observed failing before production code.
    - Existing manifests parse with default empty permissions.
    - High-risk permissions can be represented in manifests.
    - SDK helpers build manifest, extension, permission, compatibility, security review, and marketplace entry shapes.
  </acceptance_criteria>
  <verify>
    <automated>COREPACK_HOME=/tmp/corepack CHOKIDAR_USEPOLLING=true corepack pnpm exec vitest run --config vitest.config.ts packages/contracts/src/plugins/types.test.ts --pool=forks</automated>
  </verify>
</task>

</tasks>
