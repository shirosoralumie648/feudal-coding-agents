---
phase: 08-plugin-ecosystem
plan: 02
type: execute
wave: 1
depends_on: ["08-01-PLAN.md"]
files_modified:
  - apps/control-plane/src/services/plugin-security-policy.ts
  - apps/control-plane/src/services/plugin-marketplace.ts
  - apps/control-plane/src/routes/plugins.ts
  - apps/control-plane/src/routes/plugins.test.ts
autonomous: true
requirements:
  - PLG-03
  - PSC-02
must_haves:
  truths:
    - "Plugin security reviews classify risk from declared permissions"
    - "High-risk plugin enablement requires explicit admin approval"
    - "Marketplace route lists discovered and installed local plugins"
    - "Marketplace entries include compatibility and security summaries"
---

<objective>
Add local plugin marketplace/catalog routes and fail-closed plugin security controls.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add plugin security policy and marketplace routes</name>
  <files>apps/control-plane/src/services/plugin-security-policy.ts, apps/control-plane/src/services/plugin-marketplace.ts, apps/control-plane/src/routes/plugins.ts, apps/control-plane/src/routes/plugins.test.ts</files>
  <acceptance_criteria>
    - Tests are written and observed failing before production code.
    - `GET /api/plugins/:pluginId/security` returns a security review for installed plugins.
    - `POST /api/plugins/:pluginId/enable` blocks high-risk plugins without admin approval payload.
    - `GET /api/plugins/marketplace` returns installed and discovered local plugin catalog entries.
  </acceptance_criteria>
  <verify>
    <automated>COREPACK_HOME=/tmp/corepack CHOKIDAR_USEPOLLING=true corepack pnpm exec vitest run --config vitest.config.ts apps/control-plane/src/services/plugin-security-policy.test.ts apps/control-plane/src/routes/plugins.test.ts --pool=forks</automated>
  </verify>
</task>

</tasks>
