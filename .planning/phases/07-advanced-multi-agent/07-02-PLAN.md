---
phase: 07-advanced-multi-agent
plan: 02
type: execute
wave: 1
depends_on: ["07-01-PLAN.md"]
files_modified:
  - apps/acp-gateway/src/agent-scheduler/scheduler.ts
  - apps/acp-gateway/src/agent-scheduler/bottleneck-analyzer.ts
  - apps/acp-gateway/src/agent-scheduler/bottleneck-analyzer.test.ts
  - apps/acp-gateway/src/agent-scheduler/scheduler.test.ts
autonomous: true
requirements:
  - MAC-03
  - ANM-03
must_haves:
  truths:
    - "Scheduler reassigns active tasks when HeartbeatMonitor emits an unhealthy status change"
    - "Assignments that cannot be recovered move to operator_attention"
    - "BottleneckAnalyzer reports overloaded agents, missing capability capacity, unhealthy assignments, and fleet saturation"
    - "Bottleneck reports are derived snapshots and do not persist new events"
---

<objective>
Add automatic recovery and bottleneck analysis on top of the scheduler core.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduler recovery and bottleneck analyzer</name>
  <files>apps/acp-gateway/src/agent-scheduler/scheduler.ts, apps/acp-gateway/src/agent-scheduler/bottleneck-analyzer.ts, apps/acp-gateway/src/agent-scheduler/bottleneck-analyzer.test.ts, apps/acp-gateway/src/agent-scheduler/scheduler.test.ts</files>
  <acceptance_criteria>
    - Tests are written and observed failing before production code.
    - Unhealthy source agents are excluded from replacement selection.
    - Recoverable tasks become active on replacement agents.
    - Unrecoverable tasks become `operator_attention` with a reason.
    - Bottleneck analyzer returns severity, category, affected ids, and recommendation.
  </acceptance_criteria>
  <verify>
    <automated>COREPACK_HOME=/tmp/corepack CHOKIDAR_USEPOLLING=true corepack pnpm exec vitest run --config vitest.config.ts apps/acp-gateway/src/agent-scheduler/scheduler.test.ts apps/acp-gateway/src/agent-scheduler/bottleneck-analyzer.test.ts --pool=forks</automated>
  </verify>
</task>

</tasks>
