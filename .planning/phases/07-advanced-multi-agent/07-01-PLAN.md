---
phase: 07-advanced-multi-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/acp-gateway/src/agent-scheduler/types.ts
  - apps/acp-gateway/src/agent-scheduler/scheduler.ts
  - apps/acp-gateway/src/agent-scheduler/scheduler.test.ts
autonomous: true
requirements:
  - MAC-03
must_haves:
  truths:
    - "AgentScheduler assigns tasks only to capable, non-offline, non-unhealthy agents"
    - "AgentScheduler scores candidates by capacity ratio, health, missed heartbeat count, and stable agentId tie-break"
    - "Agent metadata supports optional maxConcurrentTasks and schedulingWeight"
    - "Assignment snapshots include taskId, agentId, requiredCapabilities, status, assignedAt, and score"
    - "A bounded local fixture with at least 250 agents assigns in under 200ms"
  artifacts:
    - path: "apps/acp-gateway/src/agent-scheduler/types.ts"
      provides: "Scheduler Zod schemas and TypeScript contracts"
      contains: "TaskAssignmentRequestSchema"
    - path: "apps/acp-gateway/src/agent-scheduler/scheduler.ts"
      provides: "Capacity-aware scheduler core"
      contains: "class AgentScheduler"
    - path: "apps/acp-gateway/src/agent-scheduler/scheduler.test.ts"
      provides: "Scheduler unit coverage"
      contains: "capacity ratio"
---

<objective>
Implement the scheduler core for Phase 7 distributed task assignment semantics.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduler contracts and load-aware assignment</name>
  <files>apps/acp-gateway/src/agent-scheduler/types.ts, apps/acp-gateway/src/agent-scheduler/scheduler.ts, apps/acp-gateway/src/agent-scheduler/scheduler.test.ts</files>
  <acceptance_criteria>
    - Tests are written and observed failing before production code.
    - Scheduler rejects assignments when no healthy capable agent exists.
    - Scheduler chooses the least loaded capable agent by capacity ratio.
    - Scheduler uses deterministic lexical tie-breaks.
    - Scheduler exposes `getAssignments()` and `getAgentLoads()`.
    - 250-agent local fixture assignment completes under 200ms.
  </acceptance_criteria>
  <verify>
    <automated>COREPACK_HOME=/tmp/corepack CHOKIDAR_USEPOLLING=true corepack pnpm exec vitest run --config vitest.config.ts apps/acp-gateway/src/agent-scheduler/scheduler.test.ts --pool=forks</automated>
  </verify>
</task>

</tasks>
