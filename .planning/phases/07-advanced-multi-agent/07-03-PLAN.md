---
phase: 07-advanced-multi-agent
plan: 03
type: execute
wave: 2
depends_on: ["07-01-PLAN.md", "07-02-PLAN.md"]
files_modified:
  - apps/acp-gateway/src/routes/agent-scheduler.ts
  - apps/acp-gateway/src/routes/agent-scheduler.test.ts
  - apps/acp-gateway/src/server.ts
autonomous: true
requirements:
  - MAC-03
  - ANM-03
must_haves:
  truths:
    - "ACP gateway registers /agent-scheduler routes in createGatewayApp"
    - "POST /agent-scheduler/assign creates a scheduler assignment"
    - "GET /agent-scheduler/assignments returns current assignments"
    - "GET /agent-scheduler/loads returns agent load snapshots"
    - "GET /agent-scheduler/bottlenecks returns bottleneck analysis"
    - "POST /agent-scheduler/:assignmentId/release releases an active assignment"
---

<objective>
Expose Phase 7 scheduler and bottleneck behavior through ACP gateway routes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduler routes and server wiring</name>
  <files>apps/acp-gateway/src/routes/agent-scheduler.ts, apps/acp-gateway/src/routes/agent-scheduler.test.ts, apps/acp-gateway/src/server.ts</files>
  <acceptance_criteria>
    - Tests are written and observed failing before production code.
    - Route tests cover assignment, release, load snapshot, bottleneck snapshot, and invalid payload handling.
    - `createGatewayApp()` constructs `AgentScheduler` and `BottleneckAnalyzer`.
    - Existing route tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>COREPACK_HOME=/tmp/corepack CHOKIDAR_USEPOLLING=true corepack pnpm exec vitest run --config vitest.config.ts apps/acp-gateway/src/routes/agent-scheduler.test.ts apps/acp-gateway/src/agent-scheduler/scheduler.test.ts apps/acp-gateway/src/agent-scheduler/bottleneck-analyzer.test.ts --pool=forks</automated>
  </verify>
</task>

</tasks>
