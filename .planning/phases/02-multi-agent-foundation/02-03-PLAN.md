---
phase: 02-multi-agent-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - apps/acp-gateway/src/agent-health/types.ts
  - apps/acp-gateway/src/agent-health/heartbeat-monitor.ts
  - apps/acp-gateway/src/agent-health/failover-handler.ts
  - apps/acp-gateway/src/routes/agent-health.ts
autonomous: true
requirements:
  - MAC-04
user_setup: []

must_haves:
  truths:
    - "Agents send heartbeats at configurable intervals"
    - "Missed heartbeats are detected and agents marked unhealthy"
    - "Unhealthy agents trigger failover for assigned tasks"
    - "Operators are notified of agent health events"
    - "Agent health status is visible in registry"
  artifacts:
    - path: "apps/acp-gateway/src/agent-health/types.ts"
      provides: "Type definitions for health monitoring"
      exports: ["HeartbeatConfig", "AgentHealthStatus", "HealthEvent"]
      min_lines: 40
    - path: "apps/acp-gateway/src/agent-health/heartbeat-monitor.ts"
      provides: "Heartbeat monitoring service"
      exports: ["HeartbeatMonitor", "HealthCheckResult"]
      min_lines: 120
    - path: "apps/acp-gateway/src/agent-health/failover-handler.ts"
      provides: "Automatic failover for unhealthy agents"
      exports: ["FailoverHandler", "FailoverResult"]
      min_lines: 80
    - path: "apps/acp-gateway/src/routes/agent-health.ts"
      provides: "HTTP endpoints for health operations"
      exports: ["registerAgentHealthRoutes"]
      min_lines: 50
  key_links:
    - from: "apps/acp-gateway/src/agent-health/heartbeat-monitor.ts"
      to: "apps/acp-gateway/src/agent-registry/registry.ts"
      via: "registry status updates"
      pattern: "registry\\.(updateHeartbeat|setStatus)"
    - from: "apps/acp-gateway/src/agent-health/failover-handler.ts"
      to: "apps/acp-gateway/src/agent-registry/discovery.ts"
      via: "find replacement agents by capability"
      pattern: "discovery\\.findByCapability"
---

<objective>
Implement agent heartbeat and health check system with automatic failover.

Purpose: Monitor agent health, detect failures, and automatically reassign tasks to healthy agents.
Output: Health monitoring types, heartbeat monitor, failover handler, and HTTP endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-agent-foundation/02-CONTEXT.md

<interfaces>
<!-- Types from previous plans in this phase -->

From 02-02-PLAN (Agent Registry):
```typescript
interface AgentMetadata {
  agentId: string;
  capabilities: string[];
  status: "online" | "offline" | "busy" | "unhealthy";
  lastHeartbeat: Date;
  metadata: Record<string, unknown>;
  registeredAt: Date;
  isTemporary: boolean;
}

interface AgentRegistry {
  updateHeartbeat(agentId: string, timestamp?: Date): Promise<void>;
  setStatus(agentId: string, status: AgentStatus): Promise<void>;
  getAgent(agentId: string): AgentMetadata | undefined;
}

interface AgentDiscoveryService {
  findByCapability(capability: string): AgentMetadata[];
}
```

From packages/persistence/src/event-store.ts:
```typescript
export function createPostgresEventStore(options: { pool: SqlPool })
// Methods: append, loadStream, withTransaction
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define health monitoring types and configuration</name>
  <files>apps/acp-gateway/src/agent-health/types.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-registry/types.ts
  </read_first>
  <behavior>
    - Test 1: HeartbeatConfig validates interval range (min 5s, max 300s per D-09)
    - Test 2: HealthEvent includes all required fields for audit
    - Test 3: FailoverConfig defines max retry attempts
  </behavior>
  <action>
    Create type definitions for health monitoring per D-09 through D-12:

    1. Define HeartbeatConfig interface (per D-09):
       - intervalMs: number (default 30000, configurable)
       - timeoutMs: number (time before marking missed)
       - maxMissedHeartbeats: number (default 3 per D-11)

    2. Define AgentHealthStatus enum:
       - "healthy" - receiving heartbeats
       - "degraded" - missed 1-2 heartbeats
       - "unhealthy" - missed 3+ heartbeats (per D-11)
       - "unknown" - no heartbeat received yet

    3. Define HealthEventType enum:
       - "heartbeat_received"
       - "heartbeat_missed"
       - "status_changed"
       - "failover_triggered"
       - "active_probe_sent"
       - "active_probe_failed"

    4. Define HealthEvent interface:
       - eventType: HealthEventType
       - agentId: string
       - timestamp: Date
       - previousStatus?: AgentHealthStatus
       - newStatus?: AgentHealthStatus
       - metadata?: Record<string, unknown>

    5. Define FailoverConfig interface (per D-12):
       - enabled: boolean
       - maxRetryAttempts: number
       - retryDelayMs: number
       - notifyOperator: boolean

    6. Define HealthCheckResult type for active probes

    Export all types and create Zod schemas.
  </action>
  <verify>
    <automated>npm test -- --filter=agent-health/types</automated>
  </verify>
  <done>
    - All types defined per D-09 through D-12
    - HeartbeatConfig supports configurable intervals
    - HealthStatus matches D-11 failure detection
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement heartbeat monitor with passive and active health checks</name>
  <files>apps/acp-gateway/src/agent-health/heartbeat-monitor.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-health/types.ts
    - apps/acp-gateway/src/agent-registry/registry.ts
    - packages/persistence/src/event-store.ts
  </read_first>
  <behavior>
    - Test 1: recordHeartbeat() updates agent's lastHeartbeat
    - Test 2: checkHealth() returns correct health status based on missed heartbeats
    - Test 3: After 3 missed heartbeats, agent marked unhealthy (per D-11)
    - Test 4: activeProbe() sends probe and waits for response (per D-10)
    - Test 5: Health events are logged to event store
    - Test 6: Alert callback triggered on unhealthy status (per specific idea)
  </behavior>
  <action>
    Implement heartbeat monitor per D-09 through D-11:

    1. Create HeartbeatMonitor class:
       - Constructor takes AgentRegistry, EventStore, config
       - Internal Map tracking heartbeat counts per agent
       - Timer for periodic health checks
       - Alert callback for operator notification

    2. recordHeartbeat(agentId: string, timestamp?: Date): Promise<void>
       - Update agent's lastHeartbeat in registry
       - Reset missed heartbeat counter
       - If status was unhealthy, mark as healthy
       - Emit heartbeat_received event

    3. startMonitoring(): void
       - Start periodic health check timer
       - Check at config.intervalMs

    4. stopMonitoring(): void
       - Stop health check timer
       - Cleanup resources

    5. checkHealth(agentId: string): HealthCheckResult (private)
       - Calculate time since last heartbeat
       - Return health status based on missed count
       - 0 missed: healthy
       - 1-2 missed: degraded
       - 3+ missed: unhealthy (per D-11)

    6. runHealthCheck(): Promise<void> (private, called by timer)
       - Iterate all registered agents
       - Check each agent's health
       - Update status in registry if changed
       - Emit status_changed events
       - Trigger alert callback for unhealthy agents

    7. activeProbe(agentId: string): Promise<ProbeResult> (per D-10)
       - Send ping message to agent via message router
       - Wait for response with timeout
       - Return probe result
       - Emit active_probe_sent/active_probe_failed events

    8. setAlertCallback(callback: (event: HealthEvent) => void): void
       - Register callback for health alerts
       - Notify operator on unhealthy status (per specific idea)

    Use EventEmitter pattern for health events.
  </action>
  <verify>
    <automated>npm test -- --filter=agent-health/heartbeat-monitor</automated>
  </verify>
  <done>
    - Heartbeat monitoring works per D-09
    - Passive health check via missed heartbeat count per D-10, D-11
    - Active probing supported per D-10
    - Alerts triggered for unhealthy agents
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement failover handler for automatic task reassignment</name>
  <files>apps/acp-gateway/src/agent-health/failover-handler.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-health/types.ts
    - apps/acp-gateway/src/agent-health/heartbeat-monitor.ts
    - apps/acp-gateway/src/agent-registry/registry.ts
    - apps/acp-gateway/src/agent-registry/discovery.ts
  </read_first>
  <behavior>
    - Test 1: handleFailover() finds replacement agent by capability
    - Test 2: Task reassigned to healthy agent with same capability
    - Test 3: If no replacement found, task marked for operator attention
    - Test 4: Failover events logged to event store
    - Test 5: Max retry attempts respected (per config)
  </behavior>
  <action>
    Implement failover handler per D-12:

    1. Create FailoverHandler class:
       - Constructor takes HeartbeatMonitor, AgentRegistry, AgentDiscoveryService, EventStore
       - Internal Map tracking tasks per agent

    2. registerTask(taskId: string, agentId: string, capabilities: string[]): void
       - Track which agent is handling which task
       - Store required capabilities for failover

    3. unregisterTask(taskId: string): void
       - Remove task from tracking

    4. handleFailover(unhealthyAgentId: string): Promise<FailoverResult>
       - Get all tasks assigned to unhealthy agent
       - For each task:
         a. Find replacement agent with same capability using discovery service
         b. If found, reassign task to new agent
         c. If not found, mark task for operator attention
       - Emit failover_triggered events
       - Return summary of reassignments

    5. findReplacementAgent(capabilities: string[]): AgentMetadata | undefined
       - Use discovery service to find healthy agent with matching capabilities
       - Filter by status: online or busy (not unhealthy/offline)
       - Prefer least loaded agent (basic load balancing)
       - Return undefined if no suitable agent found

    6. reassignTask(taskId: string, fromAgent: string, toAgent: string): Promise<void>
       - Update task assignment
       - Notify new agent of task
       - Log reassignment event

    7. getTasksByAgent(agentId: string): string[]
       - Return list of task IDs assigned to agent

    Connect to HeartbeatMonitor's unhealthy agent events.
  </action>
  <verify>
    <automated>npm test -- --filter=agent-health/failover-handler</automated>
  </verify>
  <done>
    - Automatic failover works per D-12
    - Tasks reassigned to healthy agents
    - No replacement triggers operator attention
  </done>
</task>

<task type="auto">
  <name>Task 4: Create HTTP endpoints for health operations</name>
  <files>apps/acp-gateway/src/routes/agent-health.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-health/heartbeat-monitor.ts
    - apps/acp-gateway/src/agent-health/failover-handler.ts
    - apps/acp-gateway/src/routes/agents.ts
    - apps/acp-gateway/src/server.ts
  </read_first>
  <action>
    Create Fastify routes for agent health API:

    1. POST /agent-health/:agentId/heartbeat
       - Record heartbeat from agent
       - Body: { timestamp? }
       - Returns: { status: "recorded" }

    2. GET /agent-health/:agentId
       - Get agent's current health status
       - Returns: { agentId, status, lastHeartbeat, missedCount }

    3. GET /agent-health
       - Get health status of all agents
       - Returns: { agents: HealthStatusSummary[] }

    4. POST /agent-health/:agentId/probe
       - Trigger active probe of agent
       - Returns: { status: "healthy" | "unhealthy", responseTime? }

    5. GET /agent-health/events
       - Get recent health events
       - Query params: ?agentId=xxx&since=timestamp
       - Returns: HealthEvent[]

    6. POST /agent-health/failover/:agentId
       - Manually trigger failover for agent
       - Body: { reason? }
       - Returns: FailoverResult

    Follow Fastify routes pattern from existing agents.ts.
    Register routes in server.ts.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/agent-health/test-agent/heartbeat returns 200</automated>
  </verify>
  <done>
    - All six endpoints registered and working
    - Routes follow existing Fastify pattern
    - OpenAPI schema documented
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Health Monitor | Agents send heartbeats, must validate agent identity |
| Health Monitor -> Registry | Status updates to registry, must be atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Spoofing | routes/agent-health.ts | mitigate | Validate agentId in heartbeat matches registered agent |
| T-02-10 | Denial of Service | heartbeat-monitor.ts | mitigate | Rate limit heartbeat submissions per agent |
| T-02-11 | Tampering | failover-handler.ts | mitigate | Log all failover decisions to event store for audit |
| T-02-12 | Repudiation | heartbeat-monitor.ts | mitigate | All health events persisted to event store |
</threat_model>

<verification>
- All unit tests pass: npm test -- --filter=agent-health
- Integration test: agent misses 3 heartbeats, marked unhealthy, failover triggered
- Event store contains health events
</verification>

<success_criteria>
- Agents send heartbeats at configurable intervals (D-09)
- 3 missed heartbeats triggers unhealthy status (D-11)
- Unhealthy agents trigger automatic failover (D-12)
- Active health probing works (D-10)
- Operators notified of health events
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-agent-foundation/02-03-SUMMARY.md`
</output>
