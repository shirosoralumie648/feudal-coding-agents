---
phase: 02-multi-agent-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/acp-gateway/src/agent-registry/types.ts
  - apps/acp-gateway/src/agent-registry/registry.ts
  - apps/acp-gateway/src/agent-registry/discovery.ts
  - apps/acp-gateway/src/routes/agent-registry.ts
autonomous: true
requirements:
  - MAC-02
user_setup: []

must_haves:
  truths:
    - "Agents can register themselves with the registry"
    - "Agents can deregister when shutting down"
    - "Operators can discover agents by capability"
    - "Agent metadata is queryable"
    - "Registry state persists across restarts"
  artifacts:
    - path: "apps/acp-gateway/src/agent-registry/types.ts"
      provides: "Type definitions for agent registry"
      exports: ["AgentMetadata", "AgentRegistration", "DiscoveryQuery"]
      min_lines: 50
    - path: "apps/acp-gateway/src/agent-registry/registry.ts"
      provides: "Agent registration and lifecycle management"
      exports: ["AgentRegistry", "RegistrationResult"]
      min_lines: 100
    - path: "apps/acp-gateway/src/agent-registry/discovery.ts"
      provides: "Agent discovery service"
      exports: ["AgentDiscoveryService", "DiscoveryResult"]
      min_lines: 80
    - path: "apps/acp-gateway/src/routes/agent-registry.ts"
      provides: "HTTP endpoints for registry operations"
      exports: ["registerAgentRegistryRoutes"]
      min_lines: 60
  key_links:
    - from: "apps/acp-gateway/src/agent-registry/registry.ts"
      to: "packages/persistence/src/event-store.ts"
      via: "registration events persistence"
      pattern: "eventStore\\.append.*agent"
    - from: "apps/acp-gateway/src/routes/agent-registry.ts"
      to: "apps/acp-gateway/src/agent-registry/registry.ts"
      via: "registry service injection"
      pattern: "registry\\.(register|unregister)"
---

<objective>
Implement agent discovery and registration service with dynamic lifecycle management.

Purpose: Enable agents to register their capabilities and be discovered by other agents or operators.
Output: Agent registry types, registration service, discovery service, and HTTP endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-agent-foundation/02-CONTEXT.md

<interfaces>
<!-- Existing patterns from worker registry -->

From apps/acp-gateway/src/workers/registry.ts:
```typescript
export const workerRegistry: Record<GatewayWorkerName, WorkerDefinition> = {
  "intake-agent": { artifactName, outputSchema, buildPrompt, parseOutput },
  "analyst-agent": { ... },
  // etc.
};
```

From apps/acp-gateway/src/workers/types.ts:
```typescript
export type GatewayWorkerName = "intake-agent" | "analyst-agent" | "fact-checker-agent" | "auditor-agent" | "critic-agent" | "gongbu-executor" | "xingbu-verifier";
```

From packages/persistence/src/event-store.ts:
```typescript
export function createPostgresEventStore(options: { pool: SqlPool })
// Methods: append, loadStream, loadAfter, readCheckpoint, writeCheckpoint
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define agent registry types and metadata schema</name>
  <files>apps/acp-gateway/src/agent-registry/types.ts</files>
  <read_first>
    - apps/acp-gateway/src/workers/types.ts
    - apps/acp-gateway/src/workers/registry.ts
  </read_first>
  <behavior>
    - Test 1: AgentMetadata validates required fields per D-06
    - Test 2: AgentRegistration includes all metadata and registration timestamp
    - Test 3: DiscoveryQuery supports capability, status, and metadata filters
    - Test 4: Temporary agent flag prevents persistence (per specific idea)
  </behavior>
  <action>
    Create type definitions for agent registry per D-05 through D-08:

    1. Define AgentStatus enum: "online" | "offline" | "busy" | "unhealthy"

    2. Define AgentMetadata interface (per D-06):
       - agentId: string (unique identifier)
       - capabilities: string[] (list of capability names)
       - status: AgentStatus
       - lastHeartbeat: Date
       - metadata: Record<string, unknown> (extensible metadata)
       - registeredAt: Date
       - isTemporary: boolean (per specific idea: auto-deregister after one-time task)

    3. Define AgentRegistration interface:
       - All fields from AgentMetadata
       - registrationId: string (internal tracking)
       - version: number (for optimistic concurrency)

    4. Define DiscoveryQuery interface:
       - capabilities?: string[] | RegExp (filter by capability)
       - status?: AgentStatus[] (filter by status)
       - metadata?: Record<string, unknown> (exact match)
       - capabilityPattern?: string (fuzzy match like 'code-*' per D-07)

    5. Define RegistrationResult type: success with agentId or error with reason

    6. Create Zod schemas for runtime validation

    Export all types and schemas.
  </action>
  <verify>
    <automated>npm test -- --filter=agent-registry/types</automated>
  </verify>
  <done>
    - All types defined per D-06
    - DiscoveryQuery supports all filter modes per D-07
    - Zod schemas validate agent metadata
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent registry with lifecycle management</name>
  <files>apps/acp-gateway/src/agent-registry/registry.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-registry/types.ts
    - apps/acp-gateway/src/workers/registry.ts
    - packages/persistence/src/event-store.ts
  </read_first>
  <behavior>
    - Test 1: register() creates new agent entry with generated ID
    - Test 2: unregister() removes agent and logs event
    - Test 3: updateHeartbeat() updates lastHeartbeat timestamp
    - Test 4: setStatus() changes agent status
    - Test 5: getAgent() returns agent metadata by ID
    - Test 6: listAgents() returns all registered agents
    - Test 7: Temporary agents are stored in memory only (per D-08)
  </behavior>
  <action>
    Implement agent registry per D-05 and D-08:

    1. Create AgentRegistry class:
       - Constructor takes event store for persistence (per D-08)
       - Internal Map for fast lookups (memory cache per D-08)
       - Separate Map for temporary agents (in-memory only)

    2. register(input: AgentRegistrationInput): Promise<RegistrationResult>
       - Generate unique agentId if not provided
       - Store in memory cache
       - If not temporary, append to event store
       - Return success with agentId

    3. unregister(agentId: string): Promise<void>
       - Remove from memory cache
       - Append deregistration event to event store
       - Clean up any mailbox references

    4. updateHeartbeat(agentId: string, timestamp?: Date): Promise<void>
       - Update lastHeartbeat in memory
       - No event store write (high frequency)

    5. setStatus(agentId: string, status: AgentStatus): Promise<void>
       - Update status in memory
       - Append status change event to event store

    6. getAgent(agentId: string): AgentMetadata | undefined
       - Return from memory cache

    7. listAgents(): AgentMetadata[]
       - Return all registered agents from memory

    8. restore(): Promise<void>
       - Load agents from event store on startup
       - Rebuild memory cache

    Use Result type pattern for error handling.
  </action>
  <verify>
    <automated>npm test -- --filter=agent-registry/registry</automated>
  </verify>
  <done>
    - Registration/unregistration works per D-05
    - Memory cache + event store persistence per D-08
    - Temporary agent support implemented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement discovery service with query capabilities</name>
  <files>apps/acp-gateway/src/agent-registry/discovery.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-registry/types.ts
    - apps/acp-gateway/src/agent-registry/registry.ts
  </read_first>
  <behavior>
    - Test 1: findByCapability() returns agents with matching capability
    - Test 2: findByStatus() returns agents with matching status
    - Test 3: findByMetadata() returns agents with matching metadata fields
    - Test 4: query() supports combined filters
    - Test 5: Fuzzy capability match 'code-*' matches 'code-generation', 'code-review' (per D-07)
  </behavior>
  <action>
    Implement discovery service per D-07:

    1. Create AgentDiscoveryService class:
       - Constructor takes AgentRegistry instance

    2. findByCapability(capability: string): AgentMetadata[]
       - Exact match on capability name
       - Return all agents with capability

    3. findByCapabilityPattern(pattern: string | RegExp): AgentMetadata[]
       - Fuzzy matching per D-07 specific idea
       - Support glob-style patterns like 'code-*'
       - Convert glob to RegExp for matching

    4. findByStatus(status: AgentStatus | AgentStatus[]): AgentMetadata[]
       - Filter agents by status
       - Support multiple statuses

    5. findByMetadata(query: Record<string, unknown>): AgentMetadata[]
       - Match agents with specific metadata fields
       - Support nested object matching

    6. query(query: DiscoveryQuery): DiscoveryResult
       - Combined filter support
       - Apply all provided filters
       - Return matched agents

    7. watch(query: DiscoveryQuery, callback: Function): Unsubscribe
       - Subscribe to registry changes
       - Notify when matching agents change status

    Export AgentDiscoveryService and DiscoveryResult type.
  </action>
  <verify>
    <automated>npm test -- --filter=agent-registry/discovery</automated>
  </verify>
  <done>
    - All discovery mechanisms per D-07 implemented
    - Fuzzy capability matching works
    - Combined query filters work
  </done>
</task>

<task type="auto">
  <name>Task 4: Create HTTP endpoints for agent registry operations</name>
  <files>apps/acp-gateway/src/routes/agent-registry.ts</files>
  <read_first>
    - apps/acp-gateway/src/agent-registry/registry.ts
    - apps/acp-gateway/src/agent-registry/discovery.ts
    - apps/acp-gateway/src/routes/agents.ts
    - apps/acp-gateway/src/server.ts
  </read_first>
  <action>
    Create Fastify routes for agent registry API:

    1. POST /agent-registry/register
       - Register new agent
       - Body: { capabilities, metadata?, isTemporary? }
       - Returns: { agentId, status }

    2. DELETE /agent-registry/:agentId
       - Unregister agent
       - Returns: { success: true }

    3. GET /agent-registry/:agentId
       - Get agent metadata by ID
       - Returns: AgentMetadata

    4. GET /agent-registry
       - List all registered agents
       - Query params: ?status=online&capability=code-*
       - Returns: AgentMetadata[]

    5. POST /agent-registry/:agentId/status
       - Update agent status
       - Body: { status }
       - Returns: { success: true }

    6. POST /agent-registry/discover
       - Discovery query endpoint
       - Body: DiscoveryQuery
       - Returns: DiscoveryResult

    Follow Fastify routes pattern from existing agents.ts.
    Register routes in server.ts.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/agent-registry/register -H "Content-Type: application/json" -d '{"capabilities":["code-generation"]}' returns 200 with agentId</automated>
  </verify>
  <done>
    - All six endpoints registered and working
    - Routes follow existing Fastify pattern
    - OpenAPI schema documented
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Registry | Agents register themselves, must validate agent identity |
| Registry -> Event Store | Registration events persisted, must handle concurrency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | registry.ts | mitigate | Validate agentId uniqueness, prevent registration takeover |
| T-02-06 | Tampering | registry.ts | mitigate | Use optimistic concurrency via version field |
| T-02-07 | Repudiation | registry.ts | mitigate | All registration changes logged to event store |
| T-02-08 | Elevation of Privilege | discovery.ts | accept | Discovery is read-only, no privilege escalation possible |
</threat_model>

<verification>
- All unit tests pass: npm test -- --filter=agent-registry
- Integration test: register agent, discover by capability
- Event store contains registration events
</verification>

<success_criteria>
- Agents can register and deregister dynamically
- Discovery supports capability, status, and metadata queries
- Fuzzy capability matching works
- Registry state persists in event store
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-agent-foundation/02-02-SUMMARY.md`
</output>
